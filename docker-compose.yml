services:
  mongodb:
    image: mongo:8.2.4
    container_name: mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE}
    volumes:
      - mongodb_data:/data/db
    deploy:
      resources:
        limits:
          cpus: "0.30"
          memory: 350M
        reservations:
          cpus: "0.10"
          memory: 150M

  backend:
    image: hoidanitreal/m05-fullstack-nest-only
    container_name: backend-nestjs
    restart: unless-stopped
    environment:
      PORT: 8000
      MONGODB_URI: mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@mongodb:27017/${MONGO_DATABASE}?authSource=admin
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN}
    depends_on:
      - mongodb
    deploy:
      resources:
        limits:
          cpus: "0.30"
          memory: 200M
        reservations:
          cpus: "0.10"
          memory: 100M

  frontend:
    image: hoidanitreal/m05-fullstack-next-only
    container_name: frontend-nextjs
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    depends_on:
      - backend
    deploy:
      resources:
        limits:
          cpus: "0.30"
          memory: 200M
        reservations:
          cpus: "0.10"
          memory: 100M

  caddy:
    image: caddy:2-alpine
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend
      - backend
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: 50M
        reservations:
          cpus: "0.05"
          memory: 20M

volumes:
  mongodb_data:
  caddy_data:
  caddy_config:

# docker compose -p hoidanit-next-nest-mongo up -d
